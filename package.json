{
  "name": "agentic-software-dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Claude Code plugin: a method for building real software with an agent.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/fuchs-harry/Agentic-software-dev.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "docs:check": "node templates/scripts/check-docs.mjs",
    "test": "node --test \"scripts/**/*.test.mjs\"",
    "check": "npm run validate && npm run docs:check && npm test"
  }
}
