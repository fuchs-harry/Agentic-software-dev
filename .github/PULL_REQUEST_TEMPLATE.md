## What this does

<One or two sentences. What changes for someone using the plugin.>

## Why

<What was missing, wrong, or unclear before.>

## Evidence

- [ ] `npm run validate` passes
- [ ] `npm test` passes
- [ ] **Negative control** (for any new check): broke `<X>` on purpose →
      `<check>` failed as expected → restored
- [ ] If a skill changed: its `description` still names the situations that
      should trigger it, in the words a user would actually type

## Not covered

<What this deliberately leaves out, and where it is tracked.>

## Checked

- [ ] Every relative link resolves (the validator checks this)
- [ ] No secrets, keys or personal data anywhere in the diff
- [ ] Conventional Commits, no `Co-Authored-By` trailer
- [ ] Advice in the diff is stated as a rule with a reason, not as a preference
